import SwiftUI

enum Theme {
    static let accent = Color(red: 0.18, green: 0.58, blue: 0.54)
    static let accentSoft = Color(red: 0.18, green: 0.58, blue: 0.54).opacity(0.2)
    static let glassStroke = Color.white.opacity(0.15)
    static let glassFill = Color.white.opacity(0.08)
    static let textPrimary = Color.primary.opacity(0.9)
    static let textSecondary = Color.primary.opacity(0.6)
}
